Place the Super Maples TTF file here so the site can use it via @font-face.

Expected filename: SuperMaples-2vR2w.ttf

If you uploaded a zip named super-maples-font.zip, extract the contained .ttf into this folder.

After placing the file, refresh the dev server. The blog signature uses the 'SuperMaples' font-face defined in Writer.css.
